## Packages
framer-motion | Page transitions and scroll-triggered animations for event cards
lucide-react | Icons for the UI (already in base, but ensuring availability)

## Notes
Tailwind Config - CSS variables use dark neon tech-fest theme (cyan and magenta accents).
The frontend handles filtering the events based on the search query and selected category from the full list returned by the API.
